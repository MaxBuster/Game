Needed:
Get winning candidate in model
Storing which round bias is purchased
Communication Simplification
Fix if 0 votes in round handler (can't count, doesn't recognize other candidates)
Change how server waits for message to check thread

Nice to have:
PCS
UI Interactions